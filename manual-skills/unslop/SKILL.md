---
name: unslop
description: Cut AI tells from user-facing prose when the user runs /unslop. Manual alias for humanizer. Do not auto-apply. Do not rewrite code. Technical documentation structure uses /technical-writing.
compatibility: opencode
license: MIT
---

# Unslop (Manual Alias for Humanizer)

Manual slash command alias for the `humanizer` specialist.

When this manual command is invoked, load and follow the canonical `humanizer` specialist body:

- In repository tree: `skills/humanizer/SKILL.md`
- After installation: `~/.config/opencode/skills/humanizer/SKILL.md`

Follow all rules, the Semantic-Preservation Constitution, and the five pattern families defined in the `humanizer` skill.

Do not maintain a separate pattern catalog here.
