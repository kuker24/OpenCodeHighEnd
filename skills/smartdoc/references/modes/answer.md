# ANSWER

Instruction → task/question manifest → authorized sources → answer each item → coverage → domain QA → output.

Build ids for numbered questions and subquestions (`q1`, `q1a`). Completion requires every required item accounted for.

Calculations when process is expected: formula → substitution → calculation → value + unit. Do not invent data.

Audience controls vocabulary. Default academic prose: clear, natural, not filler.
