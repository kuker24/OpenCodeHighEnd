# VERIFY

Completeness, calculations, citations, consistency, or Local Similarity Audit.

Findings first. If the user wants fixes: audit → findings → fix → regression. Never silently rewrite on an audit-only request.
