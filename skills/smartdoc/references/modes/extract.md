# EXTRACT

Tables, names, dates, formulae, fields, structured data.

EXTRACTION ≠ rewriting. STRICT fidelity. Do not ask identity, originality, handwriting, or audience unless the user asked for them.

Trace values to the source when practical.
