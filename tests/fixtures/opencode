#!/bin/sh
if [ "$1" = "mcp" ] && [ "$2" = "list" ]; then
  if [ -n "${OPENCODE_HE_MOCK_MCP_LIST:-}" ] && [ -f "$OPENCODE_HE_MOCK_MCP_LIST" ]; then
    cat "$OPENCODE_HE_MOCK_MCP_LIST"
    exit "${OPENCODE_HE_MOCK_MCP_LIST_RC:-0}"
  fi
  exit "${OPENCODE_HE_MOCK_MCP_LIST_RC:-0}"
fi
echo "2.0.6"
